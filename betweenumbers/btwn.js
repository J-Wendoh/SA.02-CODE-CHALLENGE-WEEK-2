function numbersBetweenArray(a,b)//the two numbers that are input by the user is a ,b 
{
    let arr = [];//the empty array 
    for(let i = a; i <= b; i++){
      arr.push(i);
    }
    return arr;//returns the numbers pushed into the array
  }
  
  return numbersBetweenArray(4,23)
  
